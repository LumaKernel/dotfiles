---
name: review-tf
description: PRに対して、Terraformコードをレビューする。
allowed-tools: Read, Glob, Grep, Agent
---

PRに対して題目の内容のレビューを行う。gh pr diffを利用する。

## 観点

1. **依存の直接性**: `local` や `var` を経由しているが、本来はリソースやデータソースを直接参照できる箇所
2. **依存の欠落**: `depends_on` を明示すべき暗黙の依存がある箇所
3. **dataリソース化**: ハードコードされた値や外部参照が `data` リソースに置き換えられる箇所

## コメント共通ルール

根拠と引用+ソースを必ず提示する。各技術用語 (クラウド関連、規格名) はすべて細かく公式ドキュメントやRFCへのリンク化をして提示する。付随して見るべきリンクがあれば最後に列挙する。推測に基いた内容は書かない。

コメントはなるべく、対応する行への直接のレビューにする。また、変更提案も可能であればするが、自明な1通りのやりかたがあるときに限定し、そうでなければ、ニュートラルに列挙のみする。

脅威に関する修正提案などは、それを実現しうる具体的な方法を一緒に提案する。
