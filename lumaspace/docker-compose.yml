version: "3.8"

services:
  entrance:
    network_mode: host
    ports:
      - "9980:8080"
    build:
      context: ./docker/entrance/
  caddy:
    network_mode: host
    build:
      context: ./docker/caddy/
